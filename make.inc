# gfortran
#export QE_ROOT=$(HOME)/install/qef-gnu
#export OPTFLAGS = -Wall # -Wextra -Wpedantic -Wsurprising

# intel
export QE_ROOT=$(HOME)/install/qef-dr
export OPTFLAGS = -warn all # -check all -g -traceback -check bounds -O0
